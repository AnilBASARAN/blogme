{
  "compilerOptions": {
    "baseUrl": ".", // This tells the compiler to resolve paths from the root
    "paths": {
      "@/*": ["src/*"] // Adjust the path if "src" is your root folder
    }
  }
}
